services:
  - type: web
    name: crisis-transport-api
    env: python
    buildCommand: |
      apt-get update && apt-get install -y gcc g++ build-essential python3-dev && pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
